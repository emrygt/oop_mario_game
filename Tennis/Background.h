#pragma once
#include "Common.h"

class Background {

	Texture pipe, pipeS, floor;
	Sprite pipe_texture1, pipe_texture2, pipeS_texture1, pipeS_texture2, floor_texture1, floor_texture2;
public:
	Background();
};